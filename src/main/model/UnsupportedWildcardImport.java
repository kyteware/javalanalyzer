package model;

// tried to parse a wildcard import, not supported. runtime error because i only handle
// this in a few places
public class UnsupportedWildcardImport extends RuntimeException {
    
}
